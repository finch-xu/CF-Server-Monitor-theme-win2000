# 本地开发：把 /api 代理到自己的 Worker（npm run dev）
VITE_DEV_PROXY_TARGET=https://status.example.com

# 纯静态部署（npm run build:github-page）
# 后端地址，多个用英文逗号分隔；每个 Worker 都要在 CORS_ALLOWED_ORIGINS 中加入主题所在域名
API_BASE=https://status.example.com
TITLE=My Server Monitor
BACKGROUND_IMAGE=
BACKGROUND_IMAGE_MOBILE=
